name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Validate curated data
      run: plamoindex curated validate curated/

    - name: Build dataset smoke test
      run: |
        plamoindex build \
          --source all \
          --curated curated/ \
          --raw data/raw-ci/ \
          --dist dist-ci/ \
          --config plamoindex.yml
        plamoindex validate --dist dist-ci/

    - name: Lint with ruff
      run: ruff check src/ tests/

    - name: Type check with mypy
      run: mypy src/

    - name: Test with pytest
      run: python -m pytest -v
